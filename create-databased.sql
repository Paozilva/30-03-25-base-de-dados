create database `silva`;
# a linha acima serve para criar um banco de dados com o nome `silva`
# ouso da crease se torna necessário para incicar uma `string`do sistema
# `strings` so sitema: `database`, `table` e `campo`
